# TODO

## 已完成

- [x] **#2 抓取状态提示** — `GET /api/status` 返回各游戏事件数，底部显示各游戏数据概览
- [x] **#3 已结束活动天数显示** — 改为 `X 天前结束` 格式
- [x] **#4 添加 favicon** — SVG 图标，浏览器标签页可见
- [x] **#5 卡片点击弹窗** — 点击卡片弹出 Modal，显示 Banner 图片、游戏名、活动标题、时间范围、详情链接
- [x] **#6 Dockerfile 构建路径** — 环境变量 `FRONTEND_DIR=/app/frontend` 覆盖路径
- [x] **#7 服务优雅关闭** — `SIGTERM`/`SIGINT` 处理，关闭调度器后退出
- [x] **#8 全局时间线视图** — 分组 / 时间线两种视图切换，按日期分组的垂直时间轴
- [x] **#9 修复一批 bug** — 终末地时区（UTC→北京时间）、随机 id 稳定化、绝区零抓取并发化、空时间过滤、图片下载走代理、方舟 banner 按段落归属、卡片 XSS 注入、天数文案与计算、暗色模式统计栏、错误提示 toast
- [x] **#10 数据持久化** — `DATA_DIR` 环境变量支持，修复容器卷挂载未生效导致的重启丢数据
- [x] **#11 终末地数据源** — end.wiki 域名失效，切换为官网公告（中文标题 + 官方 banner），结束时间从 Endfield Talos Wiki 补全
- [x] **#12 README 重写** — GitHub 展示风格，about 弹窗 markdown 渲染器升级（代码块 / 行内代码 / 粗体 / 链接 / 有序列表）

## 不做

- [ ] ~~#1 前端自动刷新~~ — 已确认不做

## 待定

- [ ] 暗色模式切换按钮在移动端位置优化
- [ ] 更多游戏数据源稳定化
- [ ] 推送通知（活动即将结束提醒）
- [ ] iCal 导出（活动加入日历）
- [ ] 多语言支持
